<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieBooking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Общие настройки темы (как для светлой, так и для темной) -->
        <item name="colorPrimary">@color/Beige</item>
        <item name="colorPrimaryVariant">@color/Beige</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Default values. -->
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="Theme.MovieBooking" parent="Base.Theme.MovieBooking" />
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.MovieBooking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base.Theme.MovieBooking.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Base.Theme.MovieBooking.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Base.Theme.MovieBooking.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>